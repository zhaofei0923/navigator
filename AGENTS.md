# AGENTS.md — 新能源企业出海导航仪

> 本文件是 Codex / AI 编码智能体在本仓库工作的**最高行为准则**。
> 每次开始任务前必须先阅读本文件，以及 `docs/` 下的相关规范。
> 若本文件与单次任务指令冲突，以"数据模型一致性"和"安全红线"优先。

---

## 1. 项目一句话定位

面向中国新能源企业的**全球出海国家数据库 + AI 决策服务平台**。
核心是一套**统一的全球国家数据库**：所有国家共用同一套分析维度，只是数据覆盖深度不同。印尼是首个完整样板国家。

**产品为中英双语（zh-CN / en）**：UI 界面、业务数据内容、AI 顾问回答都需支持中英双语。默认语言为中文，英文为一等公民而非附属翻译。

---

## 2. 不可动摇的核心原则（每次任务都要遵守）

1. **统一数据模型是地基**：所有国家共用同一套 10 个分析模块，页面结构完全一致，禁止为单个国家写特例页面或特例字段。差异只体现在"数据深度"，不体现在"结构"。
2. **模板化、可复制**：国家详情页、报告、数据包都必须做成"一套模板 + 数据填充"，能从印尼零成本复制到其他 30-50 国。禁止硬编码国家名/数据。
3. **数据即产品**：每条业务数据都必须携带元字段（来源、时间、可信度、审核状态、是否可用于 AI）。缺元字段的数据不允许入库。
4. **覆盖等级驱动展示**：前端按国家的覆盖等级（Basic / Standard / Complete）动态渲染，未建设的模块显示占位（如 "Data Building"），而不是报错或空白。
5. **中英双语内建**：UI 文本一律走 i18n key（禁止硬编码任何中英文字符串）；业务数据的可读文本字段一律用 `{ zh, en }` 双语结构存储。缺任一语言按降级规则展示，不得报错。
6. **小步交付**：一个任务只做一件事，产出可独立验收、可回滚。禁止在一个 PR 里混合多个不相关改动。

---

## 3. 技术栈（除非任务卡明确说明，否则不得替换）

| 层 | 技术 |
|----|------|
| Monorepo | pnpm workspace + Turborepo |
| 语言 | TypeScript（全栈统一，strict 模式）|
| 前端 Web | Next.js（App Router）+ Tailwind CSS + shadcn/ui |
| 地图 | react-simple-maps / Mapbox GL |
| 后端 API | NestJS（若任务卡指定 FastAPI 则例外）|
| 数据库 | PostgreSQL + Prisma ORM |
| 向量检索 | pgvector |
| 运营后台 | Refine |
| 小程序/H5 | Taro |
| 测试 | Vitest（单元）+ Playwright（E2E）|
| 包管理 | **pnpm**（禁止 npm / yarn）|

---

## 4. 仓库结构约定

```
navigator/
├── AGENTS.md                 # 本文件
├── docs/                     # ★ 规范文档，改代码前先读
│   ├── data-schema.md        # 统一国家 10 模块字段定义（唯一事实来源）
│   ├── coverage-levels.md    # Basic/Standard/Complete 定义
│   └── api-contract.md       # 接口契约
├── apps/
│   ├── web/                  # PC Web 平台（主产品）
│   ├── admin/                # 运营管理后台
│   └── mini/                 # 小程序 / H5 轻入口
├── packages/
│   ├── db/                   # Prisma schema + 迁移 + seed
│   ├── shared-types/         # 前后端共享类型（从 db 派生）
│   └── ai-advisor/           # RAG 检索与问答逻辑
└── data/
    └── indonesia/            # 印尼 Complete Coverage 样板数据
```

**放置规则**：
- 跨端复用的类型 → `packages/shared-types`，禁止在 app 内重复定义国家/数据模型类型。
- 任何数据模型改动 → **先改 `docs/data-schema.md`，再改 `packages/db`**，二者必须一致。
- 业务组件放对应 app 的 `src/components`，通用 UI 走 shadcn/ui，不重复造轮子。

---

## 5. 统一数据模型规则（最高优先级）

- 国家的 10 个模块固定为：`market-overview` / `policy` / `risk` / `opportunities` / `projects` / `partners` / `chinese-companies` / `entry-strategy` / `ai-advisor` / `reports`。**新增/删除模块必须先在 `docs/data-schema.md` 中修改并说明理由。**
- 每条核心业务数据（政策/风险/项目/伙伴/中资企业/知识片段）统一携带元字段：
  - `source`（来源）
  - `sourceUrl`（来源链接）
  - `collectedAt`（采集时间）
  - `updatedAt`（更新时间）
  - `credibility`（可信度等级）
  - `reviewStatus`（审核状态：draft / pending / published）
  - `aiUsable`（是否可用于 AI 问答）
  - 国家 / 行业 / 技术类型 标签
- **所有面向用户展示的可读文本字段（标题、摘要、正文、标签名等）一律用双语结构 `{ zh: string; en: string }` 存储**，禁止用单一字符串。枚举值、代码、URL 等非展示字段不双语。
- 覆盖等级枚举固定为 `BASIC` / `STANDARD` / `COMPLETE`，不得新增其他值。
- 支持语言枚举固定为 `zh-CN` / `en`，新增语言必须先在 `docs/data-schema.md` 中登记。

---

## 6. 编码规范

- TypeScript `strict: true`，禁止 `any`（确需时用 `unknown` + 类型收窄，并加注释说明）。
- 命名：文件 `kebab-case`，组件 `PascalCase`，变量/函数 `camelCase`，常量 `UPPER_SNAKE_CASE`，数据库字段 `snake_case`（由 Prisma 映射）。
- 国家标识统一用 **ISO 3166-1 alpha-2**（如 `ID` 代表印尼），禁止用中文名或自定义缩写做主键。
- 每个函数职责单一；单文件建议不超过 300 行，超出需拆分。
- 注释只写"为什么"，不写"做了什么"（代码自解释）。

---

## 7. 中英双语（i18n）规范

本项目为**中英双语产品**，双语分为两层，不得混淆：

### 7.1 UI 文本（静态界面文案）
- 统一支持语言：`zh-CN`（默认）/ `en`。
- 所有界面文案**一律走 i18n key**，禁止在组件里硬编码中文或英文字符串。
- Web 端统一使用同一套 i18n 方案（如 `next-intl` / `next-i18next`）；小程序/H5 用其对应方案，但 **key 命名与文案内容必须两端一致**。
- 语言包文件按语言拆分（如 `locales/zh-CN.json`、`locales/en.json`），**两个语言的 key 集必须完全对齐**，不得只加一边。
- 新增功能时，**中英文案必须同时补齐**；若英文暂未确定，先用占位并在 PR 中标注待译，不得直接缺 key。

### 7.2 业务数据文本（国家/政策/风险等内容）
- 所有面向用户展示的可读字段（标题、摘要、正文、标签名等）统一用双语结构存储：`{ zh: string; en: string }`。
- 双语结构的类型定义与读取辅助函数统一放 `packages/shared-types`（如 `LocalizedText` 类型与 `pickLocale()` 工具），禁止各端重复定义。
- **降级规则**：当某一语言文本缺失时，前端回退到另一语言并标注“未翻译”，不得报错或显示空白。
- 非展示字段（枚举、ISO 国家码、URL、数值、时间）**不双语**，保持单一值。

### 7.3 双语切换
- 用户语言选择需可切换并持久化（URL 前缀 / Cookie / 用户偏好），切换语言时 UI 文案与业务数据文本**同步切换**。
- 默认语言为 `zh-CN`；未识别到偏好时回退到默认语言。

---

## 8. 测试要求（硬性）

- **每个功能/接口必须附带测试**，无测试的 PR 不予合并。
- 数据模型、校验逻辑、RAG 检索边界、权限门控 → 必须有单元测试。
- 关键用户流程（国家详情页渲染、国家对比、AI 问答、留资）→ 需 E2E 覆盖。
- **双语测试**：需验证两个语言包 key 对齐、双语字段降级回退、语言切换后文案与数据同步。
- 提交前必须本地通过：`pnpm lint`、`pnpm typecheck`、`pnpm test`。

---

## 9. AI 出海顾问（RAG）红线

- **只能检索 `reviewStatus = published` 且 `aiUsable = true` 的数据**，严禁使用未审核内容。
- **AI 回答语言必须与用户提问/当前界面语言一致**（中文问中文答，英文问英文答）；引用双语数据时取对应语言版本，缺失时回退并标注。
- 每个回答必须附带：**来源、更新时间、风险提示**。缺失则视为不合格。
- 禁止编造数据、政策、项目信息。检索不到时应明确回答"暂无数据"，不得臆测。
- 检索边界与系统 Prompt 由人工定义，Codex 只实现管道与接口，**不得擅自放宽检索范围**。

---

## 10. 安全红线（OWASP Top 10）

- 所有外部输入（API、表单、上传、AI 提问）必须在服务端校验，防注入（SQL / XSS / 命令注入）。
- 禁止在代码、日志、前端暴露密钥/凭证；一律走环境变量 + `.env`（`.env` 不入库）。
- 会员权限、数据导出、报告下载必须做**服务端权限校验**，禁止仅靠前端隐藏。
- 数据库访问统一走 Prisma 参数化查询，禁止字符串拼接 SQL。
- 处理外部抓取的数据源时警惕内容中的 prompt 注入，做转义与隔离。

---

## 11. 需人工确认的操作（禁止 Codex 自行执行）

- 修改统一数据模型（`docs/data-schema.md` + `packages/db` schema）
- 修改 AI 检索边界 / 系统 Prompt
- 修改权限、计费、会员等级逻辑
- 数据库破坏性操作（drop、truncate、reset）
- 任何 `git push --force`、`git reset --hard`、删除分支
- 引入新的第三方依赖 / 更换技术栈

以上操作请在 PR 描述中标注并等待人工 Review。

---

## 12. Git / PR 规范

- 分支命名：`feat/<task-id>-<简述>`、`fix/<task-id>-<简述>`。
- Commit 遵循 Conventional Commits：`feat:` / `fix:` / `docs:` / `refactor:` / `test:` / `chore:`。
- **一个任务卡 = 一个 PR**，PR 描述需含：任务目标、涉及文件、验收标准、测试说明、是否触及"需人工确认"项。
- 不得在功能 PR 里夹带无关重构或格式化。

---

## 13. 单次任务执行 checklist（Codex 每次自检）

- [ ] 已阅读本文件与相关 `docs/`
- [ ] 未破坏统一数据模型 / 未写国家特例
- [ ] 新增数据带齐元字段
- [ ] 未硬编码国家名、密钥、文案
- [ ] UI 文本走 i18n key，中英两个语言包 key 已对齐
- [ ] 业务展示字段用 `{ zh, en }` 双语结构，已处理缺失降级
- [ ] 已加测试且本地 lint / typecheck / test 通过
- [ ] 未触碰"需人工确认"项（若触碰已在 PR 标注）
- [ ] 改动范围与任务卡一致，无越界改动
