# 国家详情页第3稿 · Product Design 验收记录

## 结果与边界

- 日期：2026-08-27。
- 结果：`passed`，指本次前端设计还原及工程验证，不代表任何正式数据门禁或生产发布审批。
- 运行页面：<http://127.0.0.1:3000/countries/IDN>。
- 基于现有 Navigator Demo、Next.js、Recharts、字体及 Lucide 图标实现；没有另建站点、生成替代数据或新增后台。
- 本轮改动仅涉及国家详情的前端适配、展示、测试与操作文档。原 API、数据库、原始材料及审核状态不变。

## 视觉基准与对照方法

用户选定的第3稿：

`C:/Users/12157/.codex/generated_images/01a03774-c396-7d82-b0ce-87ee4ce5aa79/exec-6a6ba271-ffd1-468f-8291-cfd202584738.png`

原图尺寸为 992 × 1586。对照状态为中文、印度尼西亚、档案和数据明细默认收起。
先在原始宽度 992 CSS px、DPR 1 检查，再检查 1440 px 桌面、390 px 手机及 320 px 英语窄屏。
Windows 浏览器滚动条占 15 px；对应文档宽度分别为 977、1425、375、305 px，均无横向溢出。

参考图和实际截图在同一轮图像输入中共同审阅，而非仅分别观看截图。
本机全页截图存在缩放/拼接异常，异常产物未用作通过证据；最终采用 992 × 900 的上下屏捕获及 DOM 尺寸测量，覆盖同一宽度下的页面结构。

截图目录：

`C:/Users/12157/.codex/visualizations/2026/08/25/01a03774-c396-7d82-b0ce-87ee4ce5aa79/`

| 证据文件 | 用途 |
| --- | --- |
| `country-detail-v3-initial-992.png` | 第一轮参考宽度对照；工具实际编码为 JPEG |
| `country-detail-v3-final-native-top.jpg` | 最终 992 px 中文首屏、双环图和宏观区开头 |
| `country-detail-v3-final-native-bottom.jpg` | 相同宽度、滚动位置 849 px，宏观区及底部评估 |
| `country-detail-v3-final-desktop-1440.jpg` | 宽屏双列布局 |
| `country-detail-v3-final-mobile-390.jpg` | 手机国家档案和能源区 |
| `country-detail-v3-final-mobile-data.jpg` | 手机柱图与完整精度明细表 |
| `country-detail-v3-mobile-en-320.jpg` | 英语窄屏、长标题与图例 |

截图只记录本地页面，没有对截图内容进行美化或替换。

## 发现、修复与复查

| 问题 | 修复 | 复查结果 |
| --- | --- | --- |
| 初版双环绘图区宽度不一致 | 平衡左右面板内边距，统一绘图/图例比例 | 992 px 下两个绘图区均为 233 × 242 px |
| 顶部留白偏大、宏观面板过高 | 收紧返回链接、页面间距和标准宽度图高，增强国家标题层级 | 保持深蓝能源区、白色双列三行面板及紧凑底部评估 |
| 纵轴出现零碎刻度或自动省略零刻度 | 显式生成圆整刻度并保留全部刻度 | 印尼增长率为 −4、−2、0、2、4、6、8；柱图保留零基线 |
| 不一致能源记录可能在摘要下标成错误单位 | 不一致/无效组合不显示数值摘要或绘制环图，原观测带原单位保留在明细 | 组件回归通过 |
| 派生差值可能有二进制尾差 | 仅派生值按输入十进制精度计算；原观测与发布比例不变 | `10.3 − 3.1 = 7.2`，科学计数法、极小值、真实零测试通过 |
| 手机固定提示的关闭目标过小 | 手机关闭按钮扩至 44 × 44 px | 页面无溢出，单点查看与明细入口可用 |

已覆盖区域没有遗留的 P0/P1/P2 视觉问题。图表、文字、图例、按钮和展开内容均没有重叠或横向挤出页面。

保留的合理差异：沿用既有 Demo 导航、标志、字体和宽屏主按钮；曲线、柱高及注记以真实 API 为准，不复制概念图的近似数据。人均 GDP 和汇率使用清楚标注的独立趋势轴，不人为增加不存在的历史数据。

## 交互与语义验证

- 实际浏览器：宏观柱图悬停显示年份、完整数值和单位；点击固定提示，移开指针仍保留，关闭按钮有效。
- 实际浏览器：能源明细包含原始总量、可再生能源、已发布比例和明确标注的计算值；宏观明细不使用万/亿缩写。
- 实际浏览器：南非保留行政、立法、司法三个首都及十二种语言；印尼保留四个 IANA 时区和行政区划。
- 实际浏览器：斯里兰卡汇率有四个有效数据点，2024 显示缺失、没有第五个点或补零连线。
- 实际浏览器：语言切换保留国家；返回地图恢复亚洲/印尼；工具和伙伴路径均保留 `country=IDN`。
- 实际浏览器：手机单列、展开表格可读；320 px 英语页面和 390 px 中文页面不横向溢出；最终图表页面无控制台错误或警告。
- 自动化测试：键盘图例 Enter/Space、Escape、TouchEnd 和点击提示、图表失败回退、缺值/真实零/负值/极端百分比、错期/错单位/重复冲突、私有路径及国家限制均通过。
- 键盘采用原生 `summary`/`button`，表格滚动区可聚焦；浏览器自动化核查了焦点及点击展开。触摸事件由组件测试覆盖，移动端点击和排版由实际浏览器覆盖；不将这些结果表述为实体手机触摸硬件测试。
- 中国/无效参数及 404/503 行为由页面、代理和后端回归测试验证；没有为模拟服务异常而停止现有后端。
- 图表代码按需加载，加载/错误/缺失状态提供对应文案；数据表位于图表错误边界之外，图表不可用时仍可阅读。

## 工程检查与部署

- `docker build --target qa -t navigator-country-charts-web-qa:local apps/web`：通过。包含生产构建、ESLint、TypeScript、61 个测试文件的 **627 项测试**；前端语句覆盖率 86.91%。
- 仓库 `.venv/bin/ruff format --check .`：160 文件通过；`.venv/bin/ruff check .`、`.venv/bin/mypy`（65 源文件）、`.venv/bin/navigator-data validate`：通过。
- `.venv/bin/pytest --cov`：**606 项通过**，覆盖率 85.77%；17 条已有依赖/SQLite 资源警告，没有测试失败。
- 当前环境无 `uv`，使用仓库现有虚拟环境等效执行。未提交代码，也未运行会重写固定数据输出的 `snapshot` / `prepare-*`；没有改变正式门禁。
- `git diff --check`：通过。`doc` 与 `data` 下原有 190 个文件前后 SHA-256 完全一致，无新增或删除。
- 仅重建并更新 `navigator-basic60-private` 项目的 web 服务，API 和 PostgreSQL 未重建/重启，三个服务健康。仍使用原来的 `127.0.0.1:3000`，没有公网部署。
- 旧 web 镜像保留为 `navigator-country-charts-before-20260827:local`，未删除历史数据。
- 操作说明已同步至 `docs/development/BASIC60_PRIVATE_RUNBOOK.md` 第12节。
