# V1.0 P0 逐步开发与验收计划

> 基线：V1.0-BASELINE
> 更新日期：2026-08-22
> 当前阶段：D0已正式通过；D1正式数据轨道与受限全栈demo轨道并行
> 生效决定：`PBD-ACCEL-DEMO-001`允许D4前启动内部合成数据全栈demo；真实数据与正式发布仍以D4为门禁

## 1. 当前结论

仓库以已经审核的文档和技术附件为基线，现已建立 D0—D4 数据就绪工具与自动测试；
中英双语全栈内部demo v2已实现，仍严格处于加速决定的合成数据边界内。D0 台账的
当前状态为：

- D0-01至D0-04的阶段任务均已完成；
- 正式评审副本已记录9个责任角色均由`kevin`承担、`peter`替补、`bob`升级，
  并已登记可复核证据；
- D0-AC-001至D0-AC-010均已由`kevin`以所需角色实名批准，最终D0决定为`approved`；
- D1—D4任务均未开始，D4量化得分为0。

### 1.1 2026-08-22加速决定

项目批准人`kevin`审核并批准SHA-256为
`86949a277cd438511b60e88f35ee0740b48f187c55183e0839f6a7307db0f86b`的
`PBD-ACCEL-DEMO-001`候选包，授权在D4前交付一个可供内部展示的完整前后端demo。
该轨道立即实施真实Web前端、FastAPI后端、数据库迁移、可重置合成种子数据、接口联调、
自动测试和一键启动；完整PRD、全量原型和正式追踪资料允许在demo后补齐。

加速决定不改变正式数据轨道：demo持续显示“演示数据 / 非正式结论”，不得连接未批准
来源、真实用户系统、外部模型或生产服务，不得公开发布，也不得把演示结果写成D1—D4
验收证据。范围内日常功能迭代只执行机器质量门禁，不再逐功能等待D1—D4人工签署。

### 1.2 中英双语全栈Demo v2实现范围

内部Demo已按“新能源企业出海工作台 / Global Expansion Workbench”重组，但该实现
仅属于`PBD-ACCEL-DEMO-001`轨道，不变更权威V1.0需求、页面、API或权限基线：

- 一级导航调整为首页、全球市场、政策与风险、出海工具和合作伙伴；首页改为低密度
  双语落地页，说明AI出海助手、光储方案、可研报告草案和项目招标四项能力；
- 全站支持`zh-CN`与`en`，默认中文，通过`navigator_locale` Cookie保持选择，不增加
  语言URL前缀；登录页、页面元数据、业务内容、错误、日期和数字格式跟随当前语言；
- 首页与全球市场使用浏览器本地WebGL、仓库内置世界国界展示几何和五国合成信息；
  支持悬停高亮、缩放/放大和单一键盘市场选择器，减少动效时停止自动旋转，WebGL
  不可用时回退为可键盘操作的选择器；
- 新增`/tools`聚合页以及`/tools/assistant`、`/tools/solar-storage`、
  `/tools/feasibility`、`/tools/tenders`四个确定性预览流程；旧`/tenders`重定向到
  `/tools/tenders`；
- `/compare`降为严格双国辅助功能，使用独立Demo接口；正式基线
  `POST /api/v1/country-comparisons`的2—4国合同保持不变；
- 新增七个`/api/v1/demo/**`只读或预览接口，均接受`locale=zh-CN|en`，并在
  `DemoMeta`中返回语言、`synthetic_demo`和双语边界提示；
- 英文界面和业务文本由仓库内静态词典与fixture提供，运行时不请求外部地图、瓦片、
  纹理、字体、翻译或模型服务；API错误保留稳定代码，由前端按语言展示；
- 工具不提供自由聊天、工程级光储设计、IRR/LCOE、正式PDF/DOCX或真实招标数据，
  也不增加业务历史表、报告存储、用户资料、任务队列或生产权限体系。

该Demo的实现与验证记录不能推导D1来源已准入、D2—D4已通过、正式P0已交付或产品
可对外发布。任何真实数据、真实客户材料、外部集成或公网暴露仍需新的书面基线决定，
或完成对应正式阶段门。

机器可验证部分已经建立并执行。当前新增发现：

- 25条核心关系的编号、关系键、权威记录、基数和约束检查通过，但涉及的34个实体中
  只有5个在字段合同中显式标记主键，AC-001机器状态为失败；
- 92个冻结字段的唯一编号、实体字段名、类型、空值和来源规则检查通过，但合同没有
  独立的单位代码或“不适用”元数据，AC-002机器状态为失败；
- 251条枚举的对象内代码唯一性、中文/转向元数据和13条迁移记录结构检查通过，
  AC-003可进入人工复核，迁移与状态转向语义仍待业务确认；
- AC-001至003分别生成独立的候选机器证据，并由哈希待审队列约束；机器失败不得
  批准，机器通过也不能代替指定实名角色复核；
- `kevin`已确认29项UUID主键、69项单位不适用、3项数值单位和20项复合字段全部拆分；
  确认记录按D0-AC-001/002分别登记证据，且不被解释为最终D0批准；
- AC-001/002整改模板现已固定29个缺主键实体、92个原字段单位决定和20个复合字段
  的原子化拆分合同。校验器
  拒绝删减、额外项目、冻结身份篡改、跨实体字段、重复新编号、不完整新增字段合同、
  拆分顺序/类型漂移、子字段单位集合不完整、部分单位元数据、非实名角色、无时区时间
  和缺失证据。通过只表示提案可进入正式
  基线变更评审，不会修改冻结Excel、改变D0结论或授权用户侧开发；
- 独立候选D0工作簿验证器已经建立：它先重放完整整改包校验，再冻结权威文件、候选
  文件和整改包哈希；保持所有未评审字段及顺序，只允许将20个已评审复合行原位替换为
  57个有序原子字段、追加29个已评审UUID主键字段和`单位`列，逐字段/单元格拒绝其他值、
  公式、表头、合同或工作表改动，并重新计算AC-001/002机器
  证据。通过只表示候选可进入正式基线评审，不会替换权威Excel或改变D0结论；
- 候选D0生成器已经建立：仅从完整且实名授权的整改包生成新`.xlsx`，继承现有表头、
  行和单位列样式；禁止覆盖、禁止写入权威`doc`目录，使用同目录临时文件生成并调用
  上述验证器，只有验证成功才原子发布，异常或验证失败均清理临时文件；
- `data/d0/candidates/d0_core_contract_candidate.2026-08-01.xlsx`已经从实名整改包生成；
  独立复算确认候选AC-001/002机器状态均为通过，本次验收已批准AC-001/002，但候选仍未激活，
  权威工作簿也未被替换；
- 批次、国家、来源及原始资料示例的引用关系检查通过；
- 6条字段映射已由数据负责人批准；门禁会在不改写只读工作簿的前提下使用正式
  评审副本中的最终目标执行模板试填，旧目标错误已消除；
- 印尼 ESDM HEESI 2024、越南 EVN Annual Report 2024-2025 和沙特 Principal
  Buyer Round 7 合格开发商名单均已完成官方来源、发布日期、采集时间、SHA-256、
  PDF安全属性和视觉核验；
- 三份样本均已形成官网页面与原件哈希绑定的许可观察快照，并由`kevin`分别以合规负责人
  和国家研究负责人身份确认`limited / false / false / approved`；合规与专业结论使用
  不同证据编号，D0-AC-005/006现已完成验收签署；
- D0正式评审模板已经建立，可一次性收集9个责任角色、6条字段映射、3条样本、
  3份候选规范、10项硬门和最终D0结论，并在提交前校验基线哈希与字段完整性。
  schema v3进一步把映射、合规/专业复核、候选规范、验收和最终批准逐项绑定到
  已签角色的实名责任人、ISO时间和证据；责任人不能作为自己的替补或升级人，
  仅填写角色标签或任意姓名不能构成批准。许可支持`approved`/`limited`/`rejected`
  三种终态，专业复核支持通过或拒绝；拒绝仍需实名签署，且许可拒绝时再分发和AI
  权限必须为否。每个证据编号还须匹配正确验收项、同一实名复核人和批准状态；样本
  合规与专业复核使用独立证据列表，拒绝跨事项或跨人员复用。
- D0当前可验收项汇总工具已经建立：它会重放正式评审包、AC-001/002整改包、独立
  候选工作簿验证、枚举机器证据、已批准规范、映射感知模板试填和三份金标准样本，
  只为满足前置条件的验收项生成精确哈希和待签证据模板；2026-08-02候选包
  `9ae32733d9ae3c973a63f382952f057b20d8a82a2bccb28d11a13a3be8bd86b7`
  将D0-AC-001至006及008列为可人工复核，保持D0-AC-007已批准，并明确阻断
  D0-AC-009/010。`kevin`已于2026-08-02确认审核该精确哈希，并以审核包所需全部角色批准
  D0-AC-001至006及008；该批准仍不激活权威工作簿或解除D4门禁。
- D0验收签署闭环已经建立：待填确认模板
  `data/d0/review/d0_acceptance_confirmation.template.2026-08-02.json`精确绑定上述候选包哈希，
  默认保持模板状态、未授权和空结论；应用命令只接受完整的001至006及008结论、已登记
  角色持有人、不早于候选包日期的ISO日期、显式落库授权和仓库内确认文件，并在原子发布新版正式评审副本及
  证据清单前重新验证确认文件、候选包、全部证据路径/哈希和评审角色绑定。拒绝结论保留
  为待整改，不会被转换为批准；该流程也不能批准009/010或修改权威工作簿。本次授权已落入
  `data/d0/evidence/kevin_acceptance_confirmation_20260802.json`，其SHA-256为
  `5ae6dd2017fc425a40c1b2d1fcc877475d69219b8fa8c4fd0607d78c751d1ce6`；应用后生成
  `data/d0/review/d0_review_packet.2026-08-02.json`并登记7条分别绑定验收项、审核人、候选包路径
  和候选包哈希的证据记录。当前正式评审为8/10项已批准；剩余D0门禁是AC-009、AC-010、
  两项缺失证据，以及权威工作簿仍存在29项主键和1项单位元数据机器阻断。
- D0候选基线采用交接已经建立：
  `data/d0/candidates/d0_baseline_adoption_review_bundle.2026-08-02.json`精确绑定当前权威
  工作簿、已验证候选、完整整改包和最新8/10正式评审，重新确认候选AC-001/002机器
  状态均为通过，并汇总29项主键、92项单位、20项复合字段拆分为57项原子字段及候选
  158项字段合同。包SHA-256为
  `b128177436355394b5a271c307f388020904b252c232b64be5fd7cb5e0ec82e2`。项目批准人
  `kevin`已于2026-08-02审核该精确哈希并决定`approved_for_manual_adoption`，授权转录
  的确认文件SHA-256为`9596ed8643a9500b7a0b8b025ffc3c7f16da87bbdf872174422d9b59c1cdcfaf`。
  决定落库器已确定性重放审核包，原子写入SHA-256为
  `b3be6e8fcef9cb3a12f0fa8f37602aa68c5789b3bf61106d68567ec195fa8748`的正式决定记录，
  并为AC-001/002登记`EVD-D0-BASELINE-ADOPTION-001-20260802`和
  `EVD-D0-BASELINE-ADOPTION-002-20260802`两条独立证据。决定记录仍明确标记
  `does_not_activate_baseline`和`does_not_complete_d0`。
- 批准后的只读发布交接验证已经通过：安全暂存副本位于权威`doc`目录之外、与权威
  工作簿同名，SHA-256为`123394d0ee1744edf86e201bd1c683cafdd222d1ff5c55221eb1f0798e2f0712`，
  与已批准候选完全一致；完整AC-001/002基线变更重放无检查项。就绪报告SHA-256为
  `fe2a6c8e8fa6abcc800adc4a80533b35dc67b087290d3ac2157c994772b80989`。当前仍须由正式
  人工基线责任流程发布修订后的权威工作簿；工具未复制、替换或激活冻结工作簿，也未
  提前批准AC-009/010或D0最终结论。
- 发布授权包已绑定批准提交`70de3b3001a8b556393a17dcf6c1c13ff7756539`中的旧权威
  工作簿、候选、暂存副本、整改包、审核包、确认、决定、证据清单和就绪报告共10个
  Git对象，包SHA-256为`1adee61309df5610da2a735902c74ed257c162ae50f076fb0582270821bb66ba`。
  发布后验证要求权威文件匹配批准哈希`123394d0…`并存在于该批准提交的后继提交；当前
  只读结果为`D0_ADOPTION_NOT_PUBLISHED`，符合冻结权威文件尚未人工发布的现场状态。
- 隔离仓库的发布后演练确认：采用验证可以通过，但合同重建会使旧正式评审的基线与
  AC-001/002输入绑定过期。`prepare-d0-post-adoption-review`现可从批准Git提交重放旧
  评审，把已签角色、6项映射、3份样本、3份规范及AC-001至008与新模板逐项比较，
  汇总12份候选哈希的变化，并生成只供项目批准人审核的拟迁移包。工具要求AC-001/002/003
  当前机器证据通过，拒绝技术附件、静态输入、提交祖先或发布哈希漂移；它不写正式评审
  副本，且始终保持AC-009/010和D0最终决定待审。
- 采用后实名确认与转录闭环已经建立：确认模板绑定迁移包SHA-256、拟评审副本SHA-256、
  唯一项目批准人和唯一输出路径；完成副本只能在`data/d0/evidence`中使用明确决定、ISO
  时间和转录授权。应用器会重放当前Git及工作簿状态，并以无覆盖的同目录临时文件原子
  写出包内拟副本；拒绝、错误审核人、回溯日期、额外字段、哈希变化或已有目标均不写出。
  该闭环只迁移既有AC-001至008实名签署，不得借此批准AC-009/010或最终D0结论。
- AC-009收口机器复核已经建立：`prepare-d0-ac009-review`只在已批准基线被后继Git提交正式
  发布、采用后评审副本成为最新版且合同快照完成重建并提交时运行。候选包绑定当前`HEAD`、
  发布授权、最新版评审、两份权威工作簿、合同清单及25份合同JSON、证据清单，并重新计算
  D0就绪报告；仅允许AC-009/010各自的待批准与缺证据四项自引用门禁存在。隔离仓库测试已
  证明正常链路通过，未提交评审、合同漂移和额外证据阻断均被拒绝。该包只准备数据负责人和
  项目批准人的AC-009具名复核，不登记批准，AC-010和D0最终结论仍须后续独立签署。
- AC-009具名确认与转录闭环已经建立：空白模板绑定候选包SHA-256、源评审Git对象、当前
  `HEAD`和唯一后续评审路径，要求数据负责人和项目批准人分别保留角色签署、实名持有人及
  与目标文件日期一致的ISO时间。每个角色使用独立证据编号。应用器只接受完整批准与显式
  转录授权，使用临时证据清单重验新版评审后原子发布评审并更新当前清单；拒绝、错误人员、
  日期不符、额外字段、重复证据、非当前清单或任何哈希/Git漂移均不落库。该流程成功时也
  只批准AC-009，AC-010与最终D0决定仍保持待审；当前仓库未伪造或预填任何真实AC-009批准。
- D0最终收口工具链已经建立：只有AC-009正式落库并提交后，`prepare-d0-final-review`才会
  接受最新版评审，并只排除AC-010待批准与缺证据两项自引用门禁；包内为AC-010验收和最终
  D0决定分别生成证据模板。最终确认要求具名项目批准人分别保留两个目的的签署。应用器使用
  临时证据清单验证无错误评审，发布候选新版评审后执行完整D0就绪重放；仅当`ready=true`、
  阻断项为0且D0任务4/4时才原子更新清单，失败会删除候选评审。隔离端到端测试证明最终状态
  可达到零阻断。当前真实仓库尚未人工发布基线，也没有AC-009/010或最终D0签署，因此仍不
  能把该演练结果解释为D0通过；即使未来D0通过，D1—D4和用户侧开发门禁仍保持独立。
- D1来源准入候选包已经建立：在18个冻结起始来源之外，已登记41个五国官方研究
  候选，国家候选数量达到印尼20、越南8、沙特8、南非8、巴西8。59个候选全部保持
  `under_review`，active数量仍为0；条款、许可、robots、访问和AI边界尚未批准，
  五国40个核心域优先/替代源组合尚待正式绑定。D1正式验证同时要求来源和组合的
  证据编号关联仓库内非空文件、实际复核人、复核时间及匹配的SHA-256。
- D2不可变原始采集候选包已经建立：6个冻结批次已生成任务合同；当前没有获准运行、
  L0原始对象或已关闭批次，D1来源未active前不得执行。运行水位、重试、停止信号、
  原件哈希、秘密日志、异常和跨批次证据均已纳入机器门禁；D2验证会重新执行完整
  D1准入、来源矩阵和仓库证据哈希检查，不能仅凭伪造的`active`状态推进。
- D3可重放处理候选包已经建立：解析、标准化和实体解析三段流水线均要求固定配置、
  代码/规则版本、参数与输入输出清单哈希。候选记录逐条绑定不可变L0对象，原文、
  译文、原值、单位/币种/时间转换、实体决策、冲突和可撤销合并均纳入机器门禁；
  D2未关闭或任何记录被标记可发布/可进入AI索引时必须失败；D3验证会重放完整
  D2→D1→D0上游校验，不能只信任阶段文件中的批准标记。
- D4质量与正式验收候选包已经建立：五国最低样本、七维100分评分、双人分层抽样、
  追溯/许可/准确率/重复率/P0硬门、种子导入/重建/回滚、来源撤权、RAG权限、
  九项移交和五类实名批准均可机器复算；当前无真实D3发布记录、演练证据或D4签署，
  因此D4仍未通过，V0.1继续阻断；D4验证会重放D3→D2→D1→D0完整上游链，不能
  依赖任一阶段文件中的自报批准状态；同时会独立重算当前P0需求、页面、API、权限
  和测试追踪报告，冻结评估哈希，并拒绝候选包自报“P0问题为0”的绕过。
- P0追踪预检已经建立：技术附件中的103项需求（其中P0为90项）、29条角色权限、
  166个页面（P0为125个）、70个API（P0合同为56个）、92个产品测试（P0为89个）、
  17个P0工程验收用例和ACC-001至ACC-011均已进入只读合同快照；
- 当前90项P0需求中仅63项关联P0测试，27项仍缺测试；125个P0页面中17个需求映射、
  93个API映射和76个测试映射仍是待补充/待细化状态。页面表使用权限代码，角色权限
  矩阵使用`PERM-*`编号，基线没有直接映射列，105种页面权限代码因此全部作为未映射
  硬阻断。以上缺口会由
  `assess-p0-traceability`持续阻断追踪准入，不能通过猜测编号消除；
- 技术附件中的页面追踪状态、API实现状态、测试执行结果和MVP验收结果均没有实际
  完成证据；机器报告分别保持0项已准入、0个已实现、0项已执行和0项已验收。
- P0追踪整改模板已生成27个需求测试项、98个页面映射项和105种权限代码项。每个
  提案必须引用已存在或显式声明的新编号，并填写变更请求、理由、复核人和日期；
  最终还需变更集与证据编号。校验通过仅代表提案可进入正式基线变更评审，不能
  改写只读工作簿或授权产品开发。
- P0候选技术附件验证器已经建立：它只接受与权威附件分离的候选文件，冻结源文件、
  候选文件和整改提案哈希，比较公式合同并拒绝删除记录、未声明编号、未评审字段、
  未授权表头及映射夹带；新增需求、API、测试或权限必须在整改包中给出完整合同记录，
  候选附件逐字段精确匹配，不能利用“编号已批准”夹带新行内容；随后在候选附件上
  重算需求、页面、API、权限和测试追踪。验证成功仅表示候选附件可进入正式基线评审，
  不会覆盖权威附件，也不会提前改变D4结论。
- P0候选技术附件生成器已经建立：仅从完整且可通过机器校验的追踪整改包生成新
  `.xlsx`，继承表头、行和Excel表样式并扩展表范围；生成前拒绝现有输出和权威
  `doc`目录，生成后逐单元格核对值与公式并重算P0追踪，只有验证成功才同目录原子
  发布，验证失败或异常均清理临时文件；
- P0交付证据包已经建立：它为90项P0需求、125个P0页面、56个P0 API、89项产品
  测试、17项工程测试和ACC-001至ACC-011分别预留实名执行/复核、Git提交和哈希证据；
  同时机器校验S0/S1、关键任务成功率、六类泄漏、AI引用覆盖、性能/容量/RPO/RTO及
  构建、部署、回滚、恢复和移交。关键任务成功率和AI引用覆盖率均由整数分子/正分母
  复算，六类泄漏必须各有正数执行样本且结果为0；第35章的18项性能、容量和灾备指标
  分别冻结比较方向、阈值、单位及需求编号，并由观测值和证据逐项验算，不能只提交
  汇总通过状态。每份证据必须绑定包含其精确内容的Git提交，当前
  文件与提交内blob哈希必须一致；实现和证据提交都必须进入最终发布提交，最终发布
  提交必须可从HEAD到达；完成包本身必须位于`data/p0/delivery`、已进入当前HEAD且
  逐字节匹配同路径Git blob。D4项目委员会的唯一实名批准人是P0签署授权根；非委员会
  ACC角色和测试负责人必须有委员会批准且带证据的授权，测试复核、ACC验收、证据批准
  和最终发布批准均逐项核对姓名与角色。每个证据还必须用稳定`subject_refs`反向声明
  所覆盖的实现、测试、验收、授权或发布主题；未知主题、缺失双向绑定和未被引用的
  游离证据都会失败。发布指标必须记录测量人和测量时间，测量时间不得晚于最终发布
  批准，并用`evaluated_commit_sha`精确绑定最终发布提交，旧提交的结果不能批准新
  版本。构建、部署、回滚、恢复和移交也必须逐项记录最终提交、执行人/时间、授权
  复核人/时间和证据，复核不得早于执行，五个自报状态不能构成完成。发布制品必须
  记录来源提交、不可变SHA-256、包含该哈希的内容寻址位置、生产人/时间和证据；指标、
  五项能力和最终批准必须在制品生产后绑定同一哈希，防止从相同提交重新构建未测试
  制品掉包。正式校验
  必须重放完整D4上游链；仓库外临时包、未提交工作区副本、任意姓名、模板、自报状态
  或游离提交不能改变当前未通过结论。
- GitHub持续集成已经覆盖锁定依赖、格式、静态类型、基线结构、合同快照、D0—D4、
  P0追踪/整改/交付模板和测试覆盖率；生成器运行前后核对源Excel哈希并拒绝任何
  合同或候选漂移。合同清单逐文件保存SHA-256，可识别行数不变的解析语义变化。

字段映射决定如下；这些决定已记录到正式评审副本，后续技术实现不得改变语义：

| 映射编号 | 原目标 | 已批准决定 |
|---|---|---|
| MAP-COUNTRY-NAME | `country.name_zh` | `model_entity → localized_text` |
| MAP-ISO3 | `country.iso3` | `replace_target → country.iso_code` |
| MAP-POLICY-DATE | `policy.published_at` | `add_field → policy_version.published_at`，与生效日分离 |
| MAP-PROJECT-CAP | `project.capacity_mw` | `add_field → project_version.capacity_value`，保留原值、单位、AC/DC侧和精度 |
| MAP-AMOUNT | `tender.budget_original` | `add_field → tender.budget_original_value`，保留币种、区间、汇率来源和基准日 |
| MAP-ENTITY | `partner.legal_name` | `model_entity → legal_entity_name` |

因此，当前可以在`PBD-ACCEL-DEMO-001`边界内维护内部全栈demo；2026-08-23完成的
Navigator Demo UI v2.1只优化登录、首页、市场、国家详情、工具和双国对比的任务旅程，
并增加本地`country=<ISO3>`上下文传递。它未改变后端合同、数据库、正式2—4国比较、
数据门或P0状态，不能解释为D1—D4、正式V0.1或P0验收。正式数据与生产交付仍按下列
阶段门推进。

## 2. 阶段一：D0 数据标准冻结

### 2.1 工具与合同基线

1. 建立只读工作簿解析器，提取实体、字段、枚举、采集规则和质量规则；
2. 对所有唯一编号、依赖、国家范围和任务状态执行结构校验；
3. 生成带源工作簿 SHA-256 的 JSON 合同快照；
4. 建立证据清单格式，校验证据路径、哈希和验收项关联；
5. 建立 D0 报告与硬门命令，未签署或缺证据时必须非零退出。

### 2.2 业务执行

1. 项目批准人补齐9个角色的姓名、替补、联系方式和签署日期；
2. 数据负责人完成核心实体、字段、主键和权威关系复核；
   先由指定角色完成`core_contract_resolution`的121项逐项提案并通过结构校验，再在
   独立候选基线中实施、复核和批准，禁止直接改写权威工作簿；
3. 产品/数据负责人冻结枚举、单位、时区、精度和多语言规则；
4. 数据质量负责人用至少一套跨实体样本试填来源、原始资料、映射、审核和D4模板；
5. 研究与审校负责人提交金标准样本和术语表；
6. 逐项提交 D0-AC-001 至 D0-AC-010 证据并由指定复核人签署。

### 2.3 D0退出标准

- D0-01至D0-04全部完成；
- 9个角色完成实名与替补签署；
- 10个D0硬门全部通过；
- 阻塞项为0，遗留问题均有责任人和目标日期；
- 项目批准人签署D0通过结论。

## 3. 阶段二：D1 来源准入

1. 对跨国来源、印尼来源及四个比较国来源逐一建档；
2. 保存当日条款、许可、访问方式、再分发和AI使用边界快照；
3. 为核心数据域登记优先来源和替代来源；
4. 禁止绕过登录、验证码、robots、速率限制或付费边界；
5. 只有准入状态为 `active` 的来源可进入自动批量采集。

退出标准：印尼不少于20个准入候选，比较国每国不少于8个候选，核心数据域均有
可执行来源和替代源，合规负责人签署准入决定。

## 4. 阶段三：D2 原始资料采集

1. 建立不可变批次号、任务号、请求参数和运行日志；
2. 保存原始网页/API/PDF/表格、响应头、抓取时间、哈希和来源版本；
3. 对失败、403、分页中断、字段漂移和访问限制进入异常台账；
4. 对敏感或无再分发权原件实施隔离和最小权限；
5. 完成五国最低样本目标，不以不可追溯记录凑数。

退出标准：原始包、清单、哈希和运行日志完整，采集可重放，无绕过访问控制行为。

## 5. 阶段四：D3 清洗与结构化

1. 解析语言、字段和原文定位，保留源文与译文绑定；
2. 标准化单位、币种、时区、日期精度和枚举；
3. 执行实体解析、确定性去重、模糊候选人工确认和可撤销合并；
4. 记录冲突事实、来源优先级、版本和变更原因；
5. 将失败记录留在候选区，不进入发布区或AI索引。

退出标准：映射可重放，所有候选有来源、版本和权限元数据，冲突与合并均可审计。

## 6. 阶段五：D4 审核、种子库与移交

1. 执行schema、必填、枚举、唯一、引用、日期、许可、时效和权限规则；
2. 对关键字段执行双人抽样，形成准确率和一致性报告；
3. 发布已审核种子数据，演练导入、重建、回滚和来源撤权；
4. 生成RAG候选，但只有已发布且有AI使用权的内容可进入索引；
5. 完成V0.1真实数据移交演练和D4正式评审。

D4硬门：

- 总分不低于85，且每个维度得分率不低于70%；
- P0数据问题为0；
- 当前P0需求、页面、API、权限和测试追踪报告通过，且评估哈希与批准证据一致；
- 印尼核心字段完整率不低于90%，四个比较国各不低于70%；
- 已发布关键事实可追溯率100%；
- 抽样准确率不低于95%，重复有效记录率不高于1%；
- 项目委员会完成D4批准签署。

## 7. 阶段六：V0.1 工程骨架

正式V0.1生产轨道仍在D4通过后执行。`PBD-ACCEL-DEMO-001`允许提前实现其中的内部
全栈demo子集，但必须使用合成数据并保持与生产、真实来源和正式账号体系隔离：

1. 按冻结单仓结构建立 `apps/web`、`apps/admin`、`services/api`、
   `services/worker`、`packages/api-client`、`packages/ui`、`packages/config` 和
   `infra`；
2. 前端采用 Node.js 24 LTS、Next.js 16 App Router、React 19、TypeScript严格模式和
   Ant Design；
3. 后端采用 Python 3.13、FastAPI 0.139兼容线、Pydantic 2、SQLAlchemy 2和Alembic；
4. 建立 PostgreSQL 18、pgvector、OpenSearch 3.7、Redis 8.8、Celery 5.6和S3兼容
   对象存储的本地环境；
5. 直接导入D4已审核种子数据，完成登录、企业上下文、国家基础查询、OpenAPI客户端、
   审计、可观测和CI；
6. 以真实种子数据完成契约、权限和端到端验证。

## 8. 阶段七：V0.5 P0 情报与AI内测

按纵向闭环交付：

1. 国家列表、国家详情和2—4国比较；
2. 政策、风险、项目、招标和伙伴列表/详情；
3. 收藏、项目详情申请、伙伴对接、咨询和撮合申请；
4. 运营审核后台、发布、下架、纠错和索引失效；
5. AI出海顾问、会话历史、权限前置检索、SSE、引用、时效和拒答；
6. 真实种子用户完成“选国家—看风险—看机会—AI分析”的内测。

退出标准：核心旅程可用，阻断缺陷为0，权限泄漏为0，AI高风险无来源结论为0。

## 9. 阶段八：V1.0 P0 完整闭环

1. 报告模板、创建、异步任务、预览、文件、额度回滚和版本冻结；
2. 企业成员、角色、会员权益、额度流水、个人安全和同意偏好；
3. CRM线索去重、A/B/C/D分级、分配、跟进、商机阶段和失败原因；
4. 用户申请与CRM双向状态、授权、审计和敏感字段披露；
5. 生产安全、限流、幂等、审计、备份恢复、可观测和故障降级；
6. 全量单元、契约、集成、E2E、数据、AI、权限、安全、性能、兼容和UAT。

最终退出标准：

- 90项P0需求实现率100%，P0测试用例100%通过；
- S0/S1缺陷为0；
- P0关键任务成功率不低于95%；
- 跨企业、搜索、缓存、文件、报告和AI泄漏为0；
- AI引用覆盖率不低于95%；
- 第35章性能、容量、RPO和RTO全部达标；
- ACC-001至ACC-011全部完成证据与签署；
- 形成可构建、可部署、可回滚、可恢复和可移交的版本。

## 10. 提交与发布策略

每个提交只完成一个可验证目标，并同时更新测试、合同和文档。阶段门未通过时不创建
虚假的“完成”提交；任何P0范围、技术栈、权限、数据来源或AI边界变更必须走正式
变更评审。通过最终验收后再标记V1.0版本并推送受保护主线。
